'use strict';
var expect = require('chai').expect;

var hashAdapter = require('../../src/adapters').hash;
var containsValidator = require('../../src/validators').contains;

describe('contains', function () {

  it('', function (done) {

    done();
  });
});