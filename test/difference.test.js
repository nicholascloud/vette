'use strict';
var expect = require('chai').expect;

var hashAdapter = require('../../src/adapters').hash;
var differenceValidator = require('../../src/validators').difference;

describe('difference', function () {

  it('', function (done) {

    done();
  });
});