'use strict';
var expect = require('chai').expect;

var hashAdapter = require('../../src/adapters').hash;
var is
-functionValidator = require('../../src/validators').is - function;

describe('is-function', function () {

  it('', function (done) {

    done();
  });
});